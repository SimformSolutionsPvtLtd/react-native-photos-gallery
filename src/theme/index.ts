import {
  scale,
  moderateScale,
  verticalScale,
  isAndroid,
  windowHeight,
  windowWidth,
  STATUS_BAR_OFFSET,
} from './Metrics';

export { default as Colors } from './Colors';
export {
  scale,
  moderateScale,
  verticalScale,
  isAndroid,
  windowHeight,
  windowWidth,
  STATUS_BAR_OFFSET,
};
